<%- include('../partials/header') %>
   <table class="table">
      <thead>
         <tr class="columns">
            <th>Seat</th>
            <th>Price</th>
         </tr>
      </thead>
      <tbody>
         <tr>
            <% if (tickets) { %>
               <% tickets.forEach( function(ticket) { %>
                  <td><%= ticket.seat %> (delete)</td>
                  <td><%= ticket.price %></td>
               <% }) %>
            <% } %>
         </tr>
      </tbody>
   </table>
   <form action="/flights/<%= flightId %>/seats" method="POST">
      <label>Seat:
         <input name="seat" required pattern="[A-F][1-9]\d?">
      </label>
      <label>Price:
         <input name="price">
      </label><br>
      <input type="submit" class="btn btn-success" value="Add Seat"><input type="button" class="btn btn-danger" onclick="location.href='/flights/<%= flightId %>'" value="Cancel">
   </form>
<%- include('../partials/footer') %>